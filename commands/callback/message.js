const Discord = require('discord.js');
// const index = require('../index.js')
exports.message = function (args) {
                // function go() { if(cuvant){ index.message.channel.send(cuvant);}};
                // const guild = new Discord.Guild();

        
                // index.message.channel.delete();
                // if(index.message.member.hasPermission("CHANGE_NICKNAME")){
                //     var cuvant = index.message.content.slice(index.command.lenght + 1);
                //go();}
                return 'Not implemented'
                }    
            
        